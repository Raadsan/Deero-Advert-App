import 'package:flutter/material.dart';

class EnterpriseNotificationpage extends StatelessWidget {
  const EnterpriseNotificationpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Welcome notification Page")),
    );
  }
}