import 'package:flutter/material.dart';

class RaadsanAboutpage extends StatelessWidget {
  const RaadsanAboutpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Welcome Raadsan Home Page"),
      ),
    );
  }
}